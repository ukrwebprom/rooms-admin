import {Box, Typography} from "@mui/material";

export default function PropertyForm() {
    return(
        <Box sx={{ display: "flex", flex: 1 }}>
            <Typography variant="h4" mb={2}>New Property</Typography>
        </Box>
    )
}